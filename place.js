const CONFIG={placesSheet:"Places"};
const $=id=>document.getElementById(id);const text=(v,f="")=>String(v??f).trim();const norm=v=>text(v).normalize("NFKC").replace(/\s+/g," ").toLowerCase();
function slugify(v){return text(v).toLowerCase().normalize("NFKD").replace(/[\u0300-\u036f]/g,"").replace(/&/g," and ").replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"")}
function placeSlug(p){return slugify(text(p["URL Slug"])||`${text(p.Place)}-${text(p.City)}`)}
function paramSlug(){const p=new URLSearchParams(location.search);return p.get("slug")||p.get("id")||location.pathname.split("/").filter(Boolean).pop()||""}
function mapLink(address){return `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(address)}`}
function isActive(p){const s=norm(p["Place Status"]);return !s||s==="active"}
function isPublishable(p){return isActive(p)&&Boolean(text(p.Place))&&Boolean(text(p.City))}
function categoryOf(p){return text(p["Public Category (Auto)"],text(p.Category,"Other"))}
function addFact(label,value){if(!value||/^(unknown|not verified)$/i.test(value))return;const dt=document.createElement("dt"),dd=document.createElement("dd");dt.textContent=label;dd.textContent=value;$("factsGrid").append(dt,dd)}
function pill(v,cls=""){if(!v)return;const s=document.createElement("span");s.className=`pill ${cls}`.trim();s.textContent=v;$("placePills").appendChild(s)}
function action(label,href,primary=false){if(!href)return;const a=document.createElement("a");a.href=href;a.target="_blank";a.rel="noopener noreferrer";a.className=`button ${primary?"place-action-primary":"place-action-secondary"}`;a.textContent=label;$("placeActions").appendChild(a)}
function relatedCard(p){const a=document.createElement("a");a.className="related-place-card";a.href=`/places/${encodeURIComponent(placeSlug(p))}`;a.innerHTML=`<strong></strong><span></span><p></p>`;a.querySelector("strong").textContent=text(p.Place);a.querySelector("span").textContent=[text(p.City),categoryOf(p)].filter(Boolean).join(" · ");a.querySelector("p").textContent=text(p["Client Notes"],text(p["Vibe / Good For"]));return a}
function setMeta(p){const name=text(p.Place),city=text(p.City),category=categoryOf(p),summary=text(p["Client Notes"],text(p["Vibe / Good For"])),canonical=`${location.origin}/places/${encodeURIComponent(placeSlug(p))}`;const title=`${name} in ${city} | Atlanta Local by Logan`;const desc=[name,category&&`is a ${category.toLowerCase()} option`,city&&`in ${city}, Georgia`,summary].filter(Boolean).join(". ").slice(0,160);document.title=title;$("metaDescription").content=desc;$("ogTitle").content=title;$("ogDescription").content=desc;$("ogUrl").content=canonical;$("canonicalLink").href=canonical}
function addSchema(p){const canonical=`${location.origin}/places/${encodeURIComponent(placeSlug(p))}`,official=text(p["Website / Source"]),addr=text(p.Address);const schema={"@context":"https://schema.org","@type":"Place",name:text(p.Place),description:text(p["Client Notes"],text(p["Vibe / Good For"])),url:canonical,sameAs:official||undefined,address:addr||undefined,containedInPlace:text(p.City)?{"@type":"City",name:text(p.City),addressRegion:"GA",addressCountry:"US"}:undefined,isPartOf:{"@type":"WebSite",name:"Atlanta Local by Logan",url:location.origin}};Object.keys(schema).forEach(k=>schema[k]===undefined&&delete schema[k]);const s=document.createElement("script");s.type="application/ld+json";s.textContent=JSON.stringify(schema);document.head.appendChild(s)}
function relatedPlacesFor(p,places){const pid=text(p["Place ID"]),city=norm(p.City),cat=norm(categoryOf(p));return places.filter(x=>isPublishable(x)&&text(x["Place ID"])!==pid&&placeSlug(x)!==placeSlug(p)&&norm(x.City)===city).sort((a,b)=>{const sameA=norm(categoryOf(a))===cat?0:1,sameB=norm(categoryOf(b))===cat?0:1;return sameA-sameB||text(a.Place).localeCompare(text(b.Place))}).slice(0,6)}
function render(p,places){setMeta(p);addSchema(p);const name=text(p.Place),category=categoryOf(p),city=text(p.City),citySlug=slugify(city),area=text(p["Area / Neighborhood"]),price=text(p.Price);$("breadcrumbName").textContent=name;if($("breadcrumbCity")){$("breadcrumbCity").textContent=city;$("breadcrumbCity").href=`/cities/${citySlug}`;}$("placeName").textContent=name;pill(city);pill(category,"category");pill(price);$("placeSummary").textContent=[area,text(p["Vibe / Good For"])].filter(Boolean).join(" · ");$("aboutText").textContent=text(p["Client Notes"],text(p["Vibe / Good For"],`${name} is included in the guide as a local option in ${city}.`));addFact("Category",text(p.Category));addFact("Neighborhood",area);addFact("Hours",text(p["Hours / Schedule"]));addFact("Pricing",text(p["Pricing Details"]));addFact("Parking",text(p.Parking));addFact("Reservations",text(p["Reservations?"]));addFact("Patio",text(p["Patio?"]));addFact("Kid friendly",text(p["Kid Friendly?"]));addFact("Dog friendly",text(p["Dog Friendly?"]));addFact("Dress level",text(p["Dress Level"]));const addr=text(p.Address),web=text(p["Website / Source"]);action("Directions ↗",addr?mapLink(addr):"",true);action("Official website ↗",web,false);const visit=[];if(addr)visit.push(`<div><strong>Address</strong><span>${addr}</span></div>`);if(text(p["Hours / Schedule"]))visit.push(`<div><strong>Hours</strong><span>${text(p["Hours / Schedule"])}</span></div>`);if(text(p["Pricing Details"]))visit.push(`<div><strong>Pricing</strong><span>${text(p["Pricing Details"])}</span></div>`);$("visitDetails").innerHTML=visit.join("");const tags=text(p["Tags / Best For"]).split(",").map(x=>x.trim()).filter(Boolean);if(tags.length){$("tagsSection").hidden=false;tags.forEach(t=>{const s=document.createElement("span");s.className="pill category";s.textContent=t;$("tagList").appendChild(s)})}if($("cityContextHeading"))$("cityContextHeading").textContent=`Explore more of ${city}.`;if($("cityContextText"))$("cityContextText").textContent=`See more restaurants, activities, shopping and local favorites around ${city}, then use Moving Here for practical relocation context.`;if($("cityContextLink")){$("cityContextLink").href=`/cities/${citySlug}`;$("cityContextLink").textContent=`Explore ${city} →`;}const related=relatedPlacesFor(p,places);if(related.length){$("relatedSection").hidden=false;related.forEach(x=>$("relatedGrid").appendChild(relatedCard(x)))}$("placeLoading").hidden=true;$("placeContent").hidden=false}
async function init(){try{if(!window.AtlantaData?.fetchSheet)throw new Error("Shared data loader did not initialize");const rows=await window.AtlantaData.fetchSheet(CONFIG.placesSheet);const places=rows.filter(isPublishable);if(!places.length)throw new Error("Places feed returned no publishable rows");const key=decodeURIComponent(paramSlug());const p=places.find(x=>norm(text(x["Place ID"]))===norm(key)||placeSlug(x)===slugify(key));if(!p)throw new Error("not found");const canonicalPath=`/places/${encodeURIComponent(placeSlug(p))}`;if(location.pathname!==canonicalPath)history.replaceState(null,"",canonicalPath);render(p,places)}catch(e){console.error(e);document.title="Place not found | Atlanta Local by Logan";$("placeLoading").hidden=true;$("placeError").hidden=false}}
document.addEventListener("DOMContentLoaded",init);